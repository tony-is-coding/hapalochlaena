# CC EE 企业级系统完整实施计划（Phase 1a + 1b）

> **给智能体工作者：** 必需的子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 来逐任务实施此计划。步骤使用复选框（`- [ ]`）语法进行跟踪。

**目标：** 构建企业级 Claude Code Agent 系统，包含多租户隔离、Token 预算管理、审计日志和 Web 前端

**架构：** 3 层系统，包含 API Gateway（Fastify）、Control Plane（租户/用户/Token 管理）和 cc_ee Service（会话编排 + cc_core 集成）。使用 AsyncLocalStorage 实现 per-session 隔离，使用 HookCallbacks 实现 token/权限强制执行。

**技术栈：** TypeScript、Fastify、PostgreSQL、Prisma、JWT、React、Vite

---

## 文件结构

此计划将创建以下结构：

```
cc_ee/
├── src/
│   ├── api-gateway/
│   │   ├── server.ts                    # Fastify 入口点
│   │   ├── auth/jwt.ts                  # JWT 中间件
│   │   └── routes/index.ts              # 路由定义
│   ├── control-plane/
│   │   ├── tenants/service.ts           # 租户 CRUD
│   │   ├── users/service.ts             # 用户 CRUD
│   │   └── tokens/service.ts            # Token 预算管理
│   ├── cc-ee-service/
│   │   ├── session/lifecycle.ts         # Session 生命周期
│   │   ├── hooks/preToolUse.ts          # PreToolUse hook
│   │   └── integration/ccCoreIntegration.ts  # cc_core 集成
│   ├── database/
│   │   ├── schema.prisma                # Prisma schema
│   │   └── seed.ts                      # 种子数据
│   └── shared/
│       ├── types/index.ts               # 共享类型
│       └── utils/index.ts               # 共享工具函数
├── package.json
├── tsconfig.json
└── .env.example

cc_ee_webui/
├── src/
│   ├── pages/
│   │   ├── Login/index.tsx              # 登录页
│   │   ├── Chat/index.tsx               # 对话界面
│   │   └── Admin/                       # 管理后台
│   ├── components/
│   │   ├── MessageList/index.tsx        # 消息列表
│   │   └── MessageInput/index.tsx       # 消息输入框
│   ├── hooks/
│   │   └── useWebSocket.ts              # WebSocket Hook
│   ├── services/
│   │   └── api.ts                       # API 客户端
│   └── store/
│       └── authStore.ts                 # 认证状态
├── package.json
└── vite.config.ts

cc_core/
└── utils/
    └── sessionState.ts                  # 新增：AsyncLocalStorage session 隔离
```

---

## Phase 1a：后端核心功能

### 任务 1：项目初始化

**文件：**
- 创建：`cc_ee/package.json`
- 创建：`cc_ee/tsconfig.json`
- 创建：`cc_ee/.env.example`

- [ ] **步骤 1：创建 cc_ee 目录并初始化 package.json**

```bash
mkdir -p cc_ee && cd cc_ee && cat > package.json << 'EOF'
{
  "name": "cc-ee",
  "version": "1.0.0",
  "description": "Claude Code Agent 企业版",
  "main": "dist/api-gateway/server.js",
  "scripts": {
    "dev": "tsx watch src/api-gateway/server.ts",
    "build": "tsc",
    "start": "node dist/api-gateway/server.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:seed": "tsx src/database/seed.ts",
    "test": "vitest"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/jwt": "^8.0.0",
    "@prisma/client": "^5.11.0",
    "fastify": "^4.26.2",
    "bcrypt": "^5.1.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/node": "^20.11.30",
    "prisma": "^5.11.0",
    "tsx": "^4.7.1",
    "typescript": "^5.4.3",
    "vitest": "^1.4.0"
  }
}
EOF
```

- [ ] **步骤 2：创建 tsconfig.json**

```bash
cat > tsconfig.json << 'EOF'
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "lib": ["ES2022"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "moduleResolution": "node",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
EOF
```

- [ ] **步骤 3：创建 .env.example**

```bash
cat > .env.example << 'EOF'
DATABASE_URL="postgresql://user:password@localhost:5432/cc_ee"
JWT_SECRET="your-secret-key-change-in-production"
PORT=3000
NODE_ENV=development
EOF
```

- [ ] **步骤 4：安装依赖**

运行：`npm install`
预期：依赖安装成功

- [ ] **步骤 5：提交**

```bash
git add package.json tsconfig.json .env.example
git commit -m "feat: 初始化 cc_ee 项目结构"
```

---

### 任务 2：数据库 Schema

**文件：**
- 创建：`cc_ee/src/database/schema.prisma`
- 创建：`cc_ee/src/database/seed.ts`

- [ ] **步骤 1：创建 Prisma schema**

```bash
mkdir -p src/database && cat > src/database/schema.prisma << 'EOF'
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Tenant {
  id                  String   @id @default(uuid())
  name                String
  status              String   @default("active")
  tokenBudgetMonthly  Int      @map("token_budget_monthly")
  enabledSkillIds     Json?    @map("enabled_skill_ids")
  permissionRules     Json?    @map("permission_rules")
  createdAt           DateTime @default(now()) @map("created_at")
  updatedAt           DateTime @updatedAt @map("updated_at")

  users         User[]
  sessions      Session[]
  tokenLedgers  TokenLedger[]
  auditLogs     ToolAuditLog[]

  @@map("tenants")
}

model User {
  id           String   @id @default(uuid())
  tenantId     String   @map("tenant_id")
  email        String   @unique
  passwordHash String   @map("password_hash")
  role         String   @default("member")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  tenant    Tenant        @relation(fields: [tenantId], references: [id])
  sessions  Session[]
  auditLogs ToolAuditLog[]

  @@map("users")
}

model Session {
  id             String    @id @default(uuid())
  tenantId       String    @map("tenant_id")
  userId         String    @map("user_id")
  workingDir     String    @map("working_dir")
  status         String    @default("active")
  nodeId         String?   @map("node_id")
  ossArchivePath String?   @map("oss_archive_path")
  createdAt      DateTime  @default(now()) @map("created_at")
  updatedAt      DateTime  @updatedAt @map("updated_at")
  lastActiveAt   DateTime? @map("last_active_at")

  tenant    Tenant         @relation(fields: [tenantId], references: [id])
  user      User           @relation(fields: [userId], references: [id])
  auditLogs ToolAuditLog[]

  @@map("sessions")
}

model TokenLedger {
  tenantId      String   @map("tenant_id")
  period        String
  totalBudget   Int      @map("total_budget")
  used          Int      @default(0)
  lastUpdatedAt DateTime @updatedAt @map("last_updated_at")

  tenant Tenant @relation(fields: [tenantId], references: [id])

  @@id([tenantId, period])
  @@map("token_ledgers")
}

model ToolAuditLog {
  id            String   @id @default(uuid())
  sessionId     String   @map("session_id")
  tenantId      String   @map("tenant_id")
  userId        String   @map("user_id")
  toolName      String   @map("tool_name")
  inputSnapshot Json?    @map("input_snapshot")
  hookDecision  String   @map("hook_decision")
  blockReason   String?  @map("block_reason")
  matchedRule   String?  @map("matched_rule")
  timestamp     DateTime @default(now())

  session Session @relation(fields: [sessionId], references: [id])
  tenant  Tenant  @relation(fields: [tenantId], references: [id])
  user    User    @relation(fields: [userId], references: [id])

  @@map("tool_audit_logs")
}

model Skill {
  id           String   @id @default(uuid())
  name         String
  slug         String   @unique
  content      String
  isOfficial   Boolean  @default(false) @map("is_official")
  allowedTools Json?    @map("allowed_tools")
  version      String
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  @@map("skills")
}
EOF
```

- [ ] **步骤 2：生成 Prisma 客户端**

运行：`npm run db:generate`
预期：Prisma 客户端生成成功

- [ ] **步骤 3：创建种子数据**

```bash
cat > src/database/seed.ts << 'EOF'
import { PrismaClient } from '@prisma/client'
import bcrypt from 'bcrypt'

const prisma = new PrismaClient()

async function main() {
  // 创建测试租户
  const tenant = await prisma.tenant.create({
    data: {
      name: '测试租户',
      status: 'active',
      tokenBudgetMonthly: 1000000,
      permissionRules: {
        deny: []
      }
    }
  })

  console.log('创建租户:', tenant.id)

  // 创建测试用户
  const passwordHash = await bcrypt.hash('password123', 10)
  const user = await prisma.user.create({
    data: {
      tenantId: tenant.id,
      email: 'test@example.com',
      passwordHash,
      role: 'admin'
    }
  })

  console.log('创建用户:', user.email)

  // 创建当前月份的 token ledger
  const period = new Date().toISOString().slice(0, 7) // YYYY-MM
  await prisma.tokenLedger.create({
    data: {
      tenantId: tenant.id,
      period,
      totalBudget: tenant.tokenBudgetMonthly,
      used: 0
    }
  })

  console.log('创建 token ledger，周期:', period)
}

main()
  .catch((e) => {
    console.error(e)
    process.exit(1)
  })
  .finally(async () => {
    await prisma.$disconnect()
  })
EOF
```

- [ ] **步骤 4：创建迁移**

运行：`npm run db:migrate`
预期：迁移创建并应用成功

- [ ] **步骤 5：运行种子数据**

运行：`npm run db:seed`
预期：种子数据创建成功

- [ ] **步骤 6：提交**

```bash
git add src/database/
git commit -m "feat: 添加数据库 schema 和种子数据"
```

---

### 任务 3：共享类型和工具函数

**文件：**
- 创建：`cc_ee/src/shared/types/index.ts`
- 创建：`cc_ee/src/shared/utils/index.ts`

- [ ] **步骤 1：创建共享类型**

```bash
mkdir -p src/shared/types && cat > src/shared/types/index.ts << 'EOF'
export interface JWTPayload {
  userId: string
  tenantId: string
  email: string
  role: string
}

export interface SessionContext {
  sessionId: string
  tenantId: string
  userId: string
  workingDir: string
}

export interface TokenUsage {
  input_tokens: number
  output_tokens: number
  cache_read_input_tokens?: number
  cache_creation_input_tokens?: number
}

export interface HookInput {
  tool_name: string
  input: Record<string, unknown>
}

export interface HookOutput {
  decision: 'approve' | 'block'
  reason?: string
}
EOF
```

- [ ] **步骤 2：创建共享工具函数**

```bash
mkdir -p src/shared/utils && cat > src/shared/utils/index.ts << 'EOF'
export function getCurrentPeriod(): string {
  return new Date().toISOString().slice(0, 7) // YYYY-MM
}

export function sanitizeInput(input: Record<string, unknown>): Record<string, unknown> {
  const sensitiveKeys = ['password', 'token', 'secret', 'key', 'apiKey', 'api_key']
  const sanitized: Record<string, unknown> = {}

  for (const [key, value] of Object.entries(input)) {
    if (sensitiveKeys.some(sk => key.toLowerCase().includes(sk))) {
      sanitized[key] = '[REDACTED]'
    } else {
      sanitized[key] = value
    }
  }

  return sanitized
}

export function matchDenyRules(
  denyRules: string[],
  input: { tool_name: string; input: Record<string, unknown> }
): string | null {
  for (const rule of denyRules) {
    const toolInput = JSON.stringify(input.input)
    if (toolInput.includes(rule)) {
      return rule
    }
  }
  return null
}
EOF
```

- [ ] **步骤 3：提交**

```bash
git add src/shared/
git commit -m "feat: 添加共享类型和工具函数"
```

---

## Phase 1b：前端接入

### 任务 4：前端项目初始化

**文件：**
- 创建：`cc_ee_webui/package.json`
- 创建：`cc_ee_webui/vite.config.ts`
- 创建：`cc_ee_webui/tsconfig.json`

- [ ] **步骤 1：创建前端项目目录并初始化**

```bash
cd .. && mkdir -p cc_ee_webui && cd cc_ee_webui && cat > package.json << 'EOF'
{
  "name": "cc-ee-webui",
  "version": "1.0.0",
  "description": "Claude Code Agent 企业版 Web UI",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.3",
    "zustand": "^4.5.2",
    "axios": "^1.6.8"
  },
  "devDependencies": {
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.4.3",
    "vite": "^5.2.0"
  }
}
EOF
```

- [ ] **步骤 2：创建 Vite 配置**

```bash
cat > vite.config.ts << 'EOF'
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true
      }
    }
  }
})
EOF
```

- [ ] **步骤 3：创建 TypeScript 配置**

```bash
cat > tsconfig.json << 'EOF'
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
EOF
```

- [ ] **步骤 4：安装依赖**

运行：`npm install`
预期：依赖安装成功

- [ ] **步骤 5：提交**

```bash
git add package.json vite.config.ts tsconfig.json
git commit -m "feat: 初始化前端项目结构"
```

---

## 执行交接

计划已完成并保存到 `docs/superpowers/plans/2026-04-09-cc-ee-完整实施计划.md`。两种执行选项：

**1. 子智能体驱动（推荐）** - 我为每个任务派发一个新的子智能体，任务间进行审查，快速迭代

**2. 内联执行** - 在此会话中使用 executing-plans 执行任务，批量执行并设置检查点

**选择哪种方式？**
